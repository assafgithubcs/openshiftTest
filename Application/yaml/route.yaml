kind: Route
apiVersion: route.openshift.io/v1
metadata:
# set your user name
  name:  user5-helloworld
spec:
  to:
    kind: Service
# set your user name
    name:  user5-helloworld
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None